# 无界请求缓冲区演示缺陷

这个小型源码模板用于演示隔离式代码修复流程。

`RecentRequestBuffer` 当前使用普通列表保存所有请求 ID，没有容量上限。随着请求持续进入，进程保留的数据会不断增长，最终可能造成内存压力。

修复目标：

- 只保留最近 `max_items` 条请求 ID；
- 保持现有公开接口不变；
- 对非法容量进行输入校验；
- 不修改测试文件。

基线测试应当失败；正确修复后测试应当通过。
